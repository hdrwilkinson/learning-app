# ==============================================
# Environment Variables Example
# ==============================================
# Copy this file to .env.local and fill in your values
# Command: cp env.example .env.local
#
# NEVER commit .env.local to git!
# ==============================================

# ----------------------------------------------
# DATABASE (Prisma)
# ----------------------------------------------
# Prisma uses two URLs:
# - DATABASE_URL: pooled runtime connection used by Prisma Client in your app
# - DIRECT_URL:   direct/non-pooled connection used by prisma migrate
#
# If you want local and production to share the SAME remote database:
# - Put the pooled (runtime) DSN in DATABASE_URL
# - Put the non-pooled (direct) DSN in DIRECT_URL
#
# Examples:
#   Prisma Accelerate (pooled runtime)
#     DATABASE_URL="prisma+postgres://accelerate.prisma-data.net/?api_key=YOUR_ACCELERATE_API_KEY"
#   Prisma (direct, non-pooled)
#     DIRECT_URL="postgres://USER:PASSWORD@db.prisma.io:5432/postgres?sslmode=require"
#
#   Vercel Postgres (alternative)
#     DATABASE_URL="<POSTGRES_PRISMA_URL>"        # pooled
#     DIRECT_URL="<POSTGRES_URL_NON_POOLING>"     # direct
#
DATABASE_URL="<POOLED_RUNTIME_DSN>"
DIRECT_URL="<DIRECT_NON_POOLED_DSN>"

# ----------------------------------------------
# AUTH.JS (NextAuth.js v5)
# ----------------------------------------------
# Base URL of your application
# Local: http://localhost:3000
# Production: https://yourdomain.com
NEXTAUTH_URL="http://localhost:3000"

# Secret key for encrypting tokens and sessions
# Generate with: openssl rand -base64 32
# IMPORTANT: Use a different secret in production!
NEXTAUTH_SECRET="generate-a-secret-with-openssl-rand-base64-32"

# Public app URL (used for password reset links)
# Local: http://localhost:3002 (matches web app port)
# Production: https://yourdomain.com
NEXT_PUBLIC_APP_URL="http://localhost:3002"

# ----------------------------------------------
# OAUTH PROVIDERS (Optional)
# ----------------------------------------------
# Google OAuth - Web Application (Next.js/NextAuth)
# Get credentials from: https://console.cloud.google.com/apis/credentials
# Create OAuth 2.0 Client ID -> Web application
# Authorized redirect URIs:
#   - http://localhost:3000/api/auth/callback/google (development)
#   - https://yourdomain.com/api/auth/callback/google (production)
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""

# Google OAuth - Mobile (Expo)
# Get credentials from: https://console.cloud.google.com/apis/credentials
# Create separate OAuth 2.0 Client IDs for Android and iOS
# Note: These are prefixed with EXPO_PUBLIC_ so they're available in the mobile app
# Android: Use package name "com.hdrwilkinson.learningapp" and SHA-1 fingerprint
# iOS: Use bundle ID "com.hdrwilkinson.learningapp"
# These should be set in apps/mobile/.env (see below for mobile env setup)
EXPO_PUBLIC_GOOGLE_CLIENT_ID=""
EXPO_PUBLIC_GOOGLE_CLIENT_ID_ANDROID=""
EXPO_PUBLIC_GOOGLE_CLIENT_ID_IOS=""

# GitHub OAuth
# Get credentials from: https://github.com/settings/developers
GITHUB_CLIENT_ID=""
GITHUB_CLIENT_SECRET=""

# ----------------------------------------------
# EMAIL (Resend)
# ----------------------------------------------
# Resend API Key for sending emails (password reset, etc.)
# Get from: https://resend.com/api-keys
# Sign up at: https://resend.com
RESEND_API_KEY=""

# ----------------------------------------------
# API CONFIGURATION
# ----------------------------------------------
# Public API base URL (accessible from client-side)
# Local development:
NEXT_PUBLIC_API_URL="http://localhost:3000/api"
# Production:
# NEXT_PUBLIC_API_URL="https://yourdomain.com/api"

# Mobile App API URL (for Expo)
# Local development (use your local IP for physical devices):
# EXPO_PUBLIC_API_URL="http://192.168.1.XXX:3000/api"
# Or use ngrok/tunnel for testing:
# EXPO_PUBLIC_API_URL="https://your-ngrok-url.ngrok.io/api"
# Production:
# EXPO_PUBLIC_API_URL="https://yourdomain.com/api"
EXPO_PUBLIC_API_URL="http://localhost:3000/api"

# ----------------------------------------------
# AI/LLM CONFIGURATION - Google Gemini
# ----------------------------------------------
# Google Gemini API Key (for GenAI-powered learning features)
# Get from: https://ai.google.dev/ (Google AI Studio)
GOOGLE_API_KEY=""

# Default Gemini model to use
# Recommended: gemini-2.5-flash-lite (fastest, cheapest, best for high-volume)
# Alternatives: gemini-1.5-pro (more capable for complex reasoning)
GEMINI_MODEL="gemini-2.5-flash-lite"

# Optional: Google Cloud Project ID (if using Vertex AI instead of AI Studio)
# GOOGLE_CLOUD_PROJECT=""

# ----------------------------------------------
# OPTIONAL: ANALYTICS & MONITORING
# ----------------------------------------------
# Add these when you need them:
# VERCEL_ANALYTICS_ID=""
# SENTRY_DSN=""
# POSTHOG_API_KEY=""

# ----------------------------------------------
# NODE ENVIRONMENT
# ----------------------------------------------
# Usually set automatically, but you can override:
# NODE_ENV="development"

